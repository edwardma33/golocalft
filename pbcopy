FROM golang:1.25 AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o app

# Step 2: Run
FROM linux:alpine

WORKDIR /app
COPY --from=builder /app/app .
COPY /client/dist /client/

CMD ["./app"]
